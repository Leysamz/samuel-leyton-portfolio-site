function showDetails(projectId) {
    const projectDetails = {
        todo: "To-Do App is a task management application built with HTML, CSS, and JavaScript.",
        weather: "Weather Dashboard provides current weather details using OpenWeather API."
    };

    alert(projectDetails[projectId]);
}

